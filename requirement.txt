# ── Norwegian Hydro-Energy Pipeline ──────────────────
# Python 3.12.10

# ── Week 1 — Extract + Transform ─────────────────────
requests==2.32.5          # HTTP API calls — NVE HydAPI
pandas==3.0.3             # Data cleaning and reshaping
python-dotenv==1.1.0      # Read .env file for secrets

# ── Week 2 — Load ─────────────────────────────────────
sqlalchemy==2.0.41        # Connect Python to PostgreSQL
psycopg2-binary==2.9.10   # PostgreSQL driver

# ── Week 3 — Orchestration ────────────────────────────
# apache-airflow==3.0.2   # Install via Docker — not pip
# See airflow/docker-compose.yml

# ── Week 6 — Transform inside DB ──────────────────────
dbt-core==1.11.11         # SQL transformation framework
dbt-postgres==1.10.0      # dbt PostgreSQL adapter

# ── Week 7 — Big data processing ──────────────────────
pyspark==4.0.0            # Distributed data processing

# ── Week 8 — Dashboard ────────────────────────────────
streamlit==1.45.1         # Interactive web dashboard
plotly==6.1.2             # Charts and visualisations