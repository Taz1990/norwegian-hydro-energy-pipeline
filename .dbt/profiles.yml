hydro_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgre_db
      user: "{{ env_var('PG_USER') }}"
      password: "{{ env_var('PG_PASSWORD') }}"
      port: 5432
      dbname: "{{ env_var('PG_DATABASE') }}"
      schema: public
      threads: 4
