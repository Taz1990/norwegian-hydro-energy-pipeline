# ───────────────────────────────────────────────
# Norwegian Hydro‑Energy Pipeline
# Python 3.12.x
#
# System Requirements (not installed via pip)
# Docker >= 27.0
# Docker Compose >= 2.27
# Used to run Airflow, PostgreSQL, and supporting services
# ───────────────────────────────────────────────

# ── Extract + Transform (Python) ───────────────
requests==2.32.3            # API calls to NVE HydAPI
pandas==2.2.2               # Data cleaning + shaping
python-dotenv==1.0.1        # Load environment variables

# ── Load (Database Connectivity) ───────────────
sqlalchemy==2.0.29          # PostgreSQL ORM/connection engine
psycopg2-binary==2.9.9      # PostgreSQL driver

# ── Orchestration (Airflow via Docker) ─────────
# apache-airflow is NOT installed via pip.
# Airflow runs inside Docker containers.
# See: airflow/docker-compose.yml

# ── dbt (SQL Transformations) ───────────────────
dbt-core==1.7.6             # dbt core engine
dbt-postgres==1.7.6         # PostgreSQL adapter for dbt

# ── Dashboard (Frontend) ────────────────────────
streamlit==1.35.0           # Interactive dashboard
plotly==5.22.0              # Charts + visualizations
